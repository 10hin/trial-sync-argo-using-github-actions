apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: my-app
  name: my-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-app
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: my-app
        version: v5
    spec:
      containers:
      - image: public.ecr.aws/docker/library/nginx:mainline
        name: nginx
        ports:
          - containerPort: 80
            name: http
        resources: {}
status: {}
